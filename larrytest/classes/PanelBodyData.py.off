if (__name__ == "__main__"):
	print("Import class: from PanelBodyData import *")
	print("Exiting...")
	exit()


class PanelBodyData:
	def __init__(self, i, name, prev_start, is_new_record, is_initial_record):
		self.i = i
		self.name = name
		self.prev_start = prev_start
		self.is_new_record = is_new_record
		self.is_initial_record = is_initial_record