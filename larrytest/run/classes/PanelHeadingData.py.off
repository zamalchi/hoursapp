if (__name__ == "__main__"):
	print("Import class: from PanelBodyHeading import *")
	print("Exiting...")
	exit()


class PanelHeadingData:
	def __init__(self, i, r, is_new_record):
		self.i = i
		self.r = r
		self.is_new_record = is_new_record
